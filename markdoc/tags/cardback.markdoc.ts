import { CardBack } from '../../components';

export const cardback = {
  render: CardBack,
  description: 'Display the enclosed content as the back of a flashcard inside a Flashcard component',
  children: ['paragraph', 'tag', 'list'],
};