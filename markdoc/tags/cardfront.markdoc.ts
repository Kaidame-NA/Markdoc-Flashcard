import { CardFront } from '../../components';

export const cardfront = {
  render: CardFront,
  description: 'Display the enclosed content as the front of a flashcard inside a Flashcard component',
  children: ['paragraph', 'tag', 'list'],
};